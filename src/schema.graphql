type Query {
  hello: String!
  user(id: ID!): User!
}

type Mutation {
  signup(name: String!, email:String!, password: String!): AuthPayload!
}

type User {
  id: ID!
  name: String!
  email: String!
}

type AuthPayload {
  token: String!
  user: User!
}
